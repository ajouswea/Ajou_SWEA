# [S/W 문제해결 기본] 5일차 - Magnetic

```
- x=0부터 시작
    - piv = 1
    - flag = False
    - y = 0 부터 시작한다
        - if not flag
            if piv != board[y][x]:
                - flag = True
                - continue
        - else 
            - if piv == board[y][x]:
                - answer +=1
                - piv = board[y][x]
                - flag = False
```

```python

```

